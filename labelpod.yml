apiVersion: v1
kind: Pod
metadata:
   name: mynginxpod
   labels:
      app: facebook
spec:
   containers:
    - image: nginx
      name: mynginxcontainer
      ports:
       - containerPort: 80     
